In observable pattern Model can become Observable and View can be observer.
When Observable changes it notifies observer it's change. This makes data flow uni directional.

View<-Model<-Controller <- View  